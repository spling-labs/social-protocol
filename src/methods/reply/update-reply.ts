import { Reply } from "../../types";

/**
 * @category Reply
 * @param reply - the updated reply
 */
export default async function updatePost(reply: Reply): Promise<Reply> {
  return Promise.resolve({ text: "" });
}
